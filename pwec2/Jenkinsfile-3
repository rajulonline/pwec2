pipeline {
agent {
    node {
      dockerfile true 
      customWorkspace '/pwec2'
    }
  }
  stages {
    stage('Test') {
      steps {
        sh '''
          node --version
          git --version
          curl --version
        '''
      }
    }
  }
}
